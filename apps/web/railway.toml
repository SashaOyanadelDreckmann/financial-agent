[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm --filter @financial-agent/web build"

[deploy]
startCommand = "pnpm --filter @financial-agent/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
